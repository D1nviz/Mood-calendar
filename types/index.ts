import { IconType } from "react-icons"

export type ILink = {
  label: string,
  path: string,
  Icon: IconType
}

export type IDate  = {
  id: string,
  year: string,
  month: string,
  date: string,
  day: string
}